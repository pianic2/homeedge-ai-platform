# IHAP-55 schematic validation — 2026-09-20

**NOT EXECUTED.** No native `homeedge-edge-mainboard.kicad_sch` has been created. The direct 47 kΩ/47 kΩ TLA2024 monitor topology fails the manufacturer's supply-off analog-input limit. MP2636 external protection/NTC/input-limit and battery reverse-cell controls remain unresolved. These are circuit-design blockers before a reviewable full schematic can be captured and ERC can have useful meaning. `erc-report.txt` records this state; it is not an ERC report from KiCad.

The revision-A logical contract and GPIO/connector pin ordering remain in `hardware/edge-mainboard/schematic-contract.json` and `schematic-rev-a.md`. Neither is a native electrical schematic.

KiCad 10.0.6 is installed in the per-user Flatpak and its CLI version command returned successfully with the required host Flatpak access. A temporary unrelated KiCad demo schematic also exported a netlist successfully, proving the parser invocation works; it was discarded because it is not IHAP-55 evidence. No partial IHAP-55 capture was produced, and no partial ERC is claimed.

Before a **complete** schematic can be reviewed, select and check the supply-off-safe TLA2024 network, MP2636 protection/NTC/input-limit values and accepted reverse-cell control. Then capture a source-linked schematic and run ERC, resolving or explicitly reviewing each violation. A powered-off-protection switch such as TI TMUX1511 may be evaluated, but direct `SEL` connection to `SYS_3V3` is not a proven safe implementation. Add an explicit SEL pull-down because TPS3839 RESET is unspecified below its 0.6 V valid-output floor. The TMUX/supervisor path remains `[UNVALIDATED]`: 20 µs is only the typical falling delay, and at 0.5 A a 24 µs sensitivity interval on 22 µF nominal `SYS_3V3` capacitance implies 0.545 V droop, greater than the 0.532 V ADC headroom at the minimum supervisor threshold. No placeholder symbols or fake ERC output are acceptable.
